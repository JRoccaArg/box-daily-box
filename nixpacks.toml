[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["echo 'Backend runs with tsx - no frontend build needed'"]

[start]
cmd = "tsx --tsconfig tsconfig.app.json src/api/index.ts"
